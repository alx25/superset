[project]
name = "irex-irex_mcp_tools"
version = "0.1.0"
license = "Apache-2.0"

[tool.apache_superset_extensions.build]
# Files to include in the extension build/bundle
include = [
    "src/irex/irex_mcp_tools/**/*.py",
]
exclude = []